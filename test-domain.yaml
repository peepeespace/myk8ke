apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: httpbin
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: mined.kr
      http:
        paths:
          - backend:
              serviceName: httpbin
              servicePort: 80
            path: /
