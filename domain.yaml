apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mined-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: mined.kr
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: hw-svc
                port:
                  number: 80
    - host: www.mined.kr
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: hw-svc
                port:
                  number: 80
